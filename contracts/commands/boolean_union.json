{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "boolean_union.json",
  "title": "BooleanUnion Command",
  "description": "Combine multiple closed solids into one via boolean union",
  "type": "object",
  "properties": {
    "object_ids": {
      "allOf": [
        { "$ref": "../common/definitions.json#/$defs/guidArray" },
        { "minItems": 2 }
      ],
      "description": "GUIDs of solids to union (at least 2)"
    },
    "delete_sources": { "type": "boolean", "default": true },
    "name": { "type": "string" },
    "dry_run": {
      "type": "boolean",
      "default": false,
      "description": "Preview the result and its metrics without modifying the document"
    }
  },
  "required": ["object_ids"],
  "additionalProperties": false
}
