{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gh_batch_search_components.json",
  "title": "gh_batch_search_components",
  "description": "Search for multiple installed Grasshopper components. Returns the best legacy result plus ranked matches and ambiguous matches for each query.",
  "type": "object",
  "properties": {
    "queries": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "minItems": 1,
      "maxItems": 100
    },
    "max_matches": {
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "default": 5,
      "description": "Maximum ranked matches to return for each query."
    }
  },
  "required": ["queries"],
  "additionalProperties": false
}
